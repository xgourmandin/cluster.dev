##################################################
# GCP Secret Manager cluster.dev secret example. #
# Generated by cdev console tool.                #
##################################################
name: /{ .secret_name }/
kind: Secret
driver: gcp_secretmanager
spec:
    project_id: /{ .project_id }/
    gcp_secret_name: /{ .gcp_secret_name }/

# Example:
# Add GCP secret with console cli: gcloud secrets create /{ .gcp_secret_name }/ --replication-policy automatic
# template reference example for secret added above (depends on data in GCP Secret Manager secret): {{ .secret./{ .secret_name }/.username }} or {{ .secret./{ .secret_name }/.password }}
